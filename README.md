# Eyal Bar Mitzvah

Static luxury invitation site for GitHub Pages.

ponytail: dynamic QR via qrserver image endpoint instead of custom QR encoder; trigger: replace if offline-only QR becomes required.
